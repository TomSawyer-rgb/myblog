---
import BaseLayout from '../layouts/BaseLayout.astro';
import TypeHero from '../components/TypeHero.astro';
import SectionHead from '../components/SectionHead.astro';
import PixelIcon from '../components/PixelIcon.astro';
import PostCard from '../components/PostCard.astro';
import type { IconName } from '../components/pixel-matrices';
import { getCollection } from 'astro:content';

const posts = await getCollection('blog');
posts.sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
const latest = posts.slice(0, 5);

const pixels: { name: IconName; label: string }[] = [
  { name: 'chip', label: 'STM32' },
  { name: 'terminal', label: 'tty' },
  { name: 'keyboard', label: 'kbd' },
  { name: 'coffee', label: 'coffee' },
  { name: 'board', label: 'pcb' },
  { name: 'led', label: 'LED' },
];
---
<BaseLayout title="tomsawyer@blog:~$">
  <TypeHero title="tomsawyer@blog: ~">
    <div class="t-line t-cmd"><span class="t-prompt">$</span><span class="t-cmd-text">whoami</span></div>
    <div class="t-line t-out-accent">我是tomsawyer —— 一个忧郁的boy</div>
    <div class="t-line t-cmd"><span class="t-prompt">$</span><span class="t-cmd-text">why build a blog?</span></div>
    <div class="t-line t-out">因为我想记录我的思考和学习过程。</div>
    <div class="t-line t-cmd"><span class="t-prompt">$</span><span class="t-cmd-text">what plan to write here in the future?</span></div>
    <div class="t-line t-out">不知道，随便写写吧！</div>
  </TypeHero>

  <div class="pixel-row">
    {pixels.map((p) => (
      <div class="pixel-item">
        <PixelIcon name={p.name} />
        <span class="pixel-label">{p.label}</span>
      </div>
    ))}
  </div>

  <SectionHead cmd="ls ~/posts" comment="最新文章" />
  <div class="post-list">
    {latest.map((p, i) => (
      <PostCard
        num={i + 1}
        slug={p.id}
        title={p.data.title}
        date={p.data.date}
        category={p.data.category}
        summary={p.data.summary}
      />
    ))}
  </div>
  <div class="list-more">
    <a href="/blog/" class="more-link">cd ~/blog <span class="arrow">→</span> 查看全部文章</a>
  </div>
</BaseLayout>

<style>
  .pixel-row {
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 14px;
    margin: 26px 0 6px;
  }
  .pixel-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 8px;
  }
  .pixel-label {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--muted);
  }
  .list-more {
    padding: 26px 10px;
    text-align: right;
  }
  .more-link {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--muted);
    transition: color 0.2s;
  }
  .more-link:hover {
    color: var(--accent-dark);
  }
</style>