# 写文章指南（myblog 专用）

翻到这份就够了。三分钟入门：复制模板 → 改 frontmatter → 写正文 → 保存 → `pnpm dev` 本地看效果 → `git commit + push` 上线。

---

## 一、发文流程

**1. 建文件**：在 `src/content/blog/` 下新建 `文章标题拼音或英文.md`（文件名就是网址，例如 `freertos-task.md` → 域名后是 `/blog/freertos-task/`）。文件名**不要有空格和中文**。

**2. 贴模板**：

```markdown
---
title: 文章标题（纯文字，别加 *markdown*）
date: 2026-09-21            # 必须 YYYY-MM-DD，写错日期构建会报错
category: 学习笔记          # 只能从下面四个里选，拼错构建会报错
summary: 一句话说清这篇写了什么，显示在列表页，20~40 字
---

正文从这里开始。
```

**3. 预览**：`pnpm dev` → 打开 `localhost:4321/blog/文章文件名/`。
**4. 上线**：`git add . && git commit -m "发文: xxx" && git push`，GitHub Actions 自动构建（仓库 Actions 页能看到进度，变绿勾就上线了）。

**可选草稿**：文件名加 `draft-` 前缀（内容不重要），想彻底不让人看就直接别 push 到仓库；本模板默认没有草稿功能。

**frontmatter 红线**：`category` 只能是 `随笔` / `学习笔记` / `项目` / `生活` 之一；`date` 少一个横杠都不行。违反任何一条，`pnpm build` 会直接报错而不是把错文章推上线——这是好事。

---

## 二、常用语法速查

### 标题层级

```markdown
## 二级标题      ← 文章里最大的标题（一级 title 已由 frontmatter 生成）
### 三级标题     ← 小节
#### 四级标题
```

习惯：一篇里最多三四个 `##`，每章内部用 `###`。别在正文里写 `# 一级标题`。

### 强调

```markdown
**粗体**  *斜体*  ~~删除线~~  `行内代码`
```

例：寄存器地址 `0x40021018` 是**固定**的，但*写法*不限。

### 列表

```markdown
- 无序项一
- 无序项二

1. 第一步
2. 第二步
```

空一行结束列表。

### 引用

```markdown
> 引用一句话。
```

### 链接与图片

```markdown
[文字](https://example.com)
![图片说明](/images/pic.jpg)        ← 图片放 public/images/ 下，路径必须以 / 开头
```

链接建议写全 `https://`。**图片不要用 `../xxx` 相对路径**，一律 `public/images/` + `/images/` 开头。

### 分隔线

```markdown
---
```

### 表格（GFM）

```markdown
| 寄存器 | 地址 | 作用 |
| ------ | ---- | ---- |
| RCC_APB2ENR | 0x40021018 | 开 GPIOA 时钟 |
```

### 代码块（本站的招牌）

三个反引号 + 语言名，立即获得暗色终端窗 + 磷光绿/琥珀高亮：

````markdown
```c
uint32_t *rcc = (uint32_t *)0x40021018;
*rcc |= (1 << 2);   // 开 GPIOA 时钟
```

```bash
pnpm dev
```
````

常用语言名：`c` `bash` `python` `js` `ts` `json` `text`（无高亮）。**不写语言名 = `text` 纯灰**——想写 C 一定写 ```c。

### 任务清单

```markdown
- [x] 已做
- [ ] 待办
```

---

## 三、推荐文章结构

```markdown
---
title: xxx
date: 20xx-xx-xx
category: 学习笔记
summary: xxx
---

## 起因 / 要解决什么问题
（半段话讲清楚这篇的价值，别直接从代码开始）

## 怎么做 / 原理
（正文主体，配代码块和小节）

## 踩坑记录
（用 > 引用突出每个坑，这是最容易被收藏的段落）

## 总结
（一两句话收尾 + 一句“下次写什么”）
```

写作纪律（保持这个博客的“人味”）：一段只说一件事；代码别贴超过 30 行；不用 emoji 当章节图标；日期/版本号写清楚，未来复盘全靠它们。

---

## 四、常见错误对照表

| 现象 | 原因 | 解法 |
|---|---|---|
| `pnpm build` 报 `Invalid option: xxx` | category 拼错 | 换成四个合法分类之一 |
| 文章没出现在列表页 | 文件名/日期格式错 | 检查 `date: 2026-09-21` |
| 代码块没高亮 | 三个反引号后没写语言名 | ```c、```bash |
| 上传后 404 | 文件名有空格或中文 | 改成英文短横线 |
| `*星号*` 直接显示出来了 | 星号两侧有空格 | 写成 `*文字*` 无空格 |
| 网址带默认协议变成奇怪链接 | 文本里裸写 example.com | 写全 `https://example.com` |
| 页面里出现奇怪样式 | 正文混入 HTML 标签 | 删掉 HTML，用 Markdown 语法 |

---

## 五、进阶（熟练后再看）

- 一篇文章想分“上下篇”：两篇都写，末尾互链。
- 想引用本站其他文章：`[STM32 点灯](/blog/stm32-led/)`。
- 想放图：`public/images/` 下建子目录按文章名（如 `public/images/freertos-task/`），正文写 `/images/freertos-task/xxx.png`。
- frontmatter 还可以加自己的字段（如 `tags: [a, b]`），改 `src/content.config.ts` 的 schema 加一行即可。
